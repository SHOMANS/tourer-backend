# Environment variables (copy to .env and fill in actual values)

# Database URL for PostgreSQL connection
DATABASE_URL=postgresql://postgres:postgres@db:5432/mvpdb?schema=public

# Application port
PORT=3000

# JWT secret for authentication (use a secure random string in production)
JWT_SECRET=example_secret